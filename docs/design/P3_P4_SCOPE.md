# P3/P4 scope guard

This stage changes reusable Designer presentation primitives only. It does not change report schema, template persistence, ESC/POS/PDF rendering, drag math, undo/redo semantics, or table data behavior.
