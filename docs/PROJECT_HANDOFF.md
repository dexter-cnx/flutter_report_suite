# Flutter Report Suite — Project Handoff

Repository: `dexter-cnx/flutter_report_suite`

Reference Flutter toolchain: **Flutter 3.32.7**

Last updated: **2026-08-13**

## Mission

Maintain the production-ready offline-first report suite while continuing Designer UX improvements without weakening the validated printer/report engine boundaries.

Current structure:

```text
flutter_report_suite/
├── apps/designer
├── packages/report_engine
├── packages/report_engine_sunmi
└── docs
```

- `apps/designer` — drag/drop report designer
- `packages/report_engine` — template model, PDF, ESC/POS, printer discovery, Hive/template storage
- `packages/report_engine_sunmi` — optional Android-only Sunmi adapter

## Operating rules

1. Inspect existing code before changing it.
2. Preserve public APIs unless correctness requires a change.
3. Prefer incremental changes over broad rewrites.
4. Keep runtime offline-first; no backend is required.
5. Never weaken tests just to make CI pass.
6. Do not claim physical printer compatibility without physical evidence.
7. Hardware-dependent functionality remains `NEEDS PHYSICAL VERIFICATION` until tested on real hardware.
8. Complete validation, review, CI, and merge for one phase before starting the next phase branch.
9. Preserve the six-platform Designer build matrix.
10. Keep PDF rendering, ESC/POS rendering, transport, discovery, and hardware capabilities as separate responsibilities.
11. Designer hardware UI must be based on real `report_engine` / adapter contracts; do not invent printer status or capabilities.

---

# Production roadmap status

## PRE-FLIGHT — Repository Audit

**Status: ✅ COMPLETE — 2026-08-12**

## PHASE 1 — FOUNDATION

**Status: ✅ MERGED / COMPLETE**

Delivered runnable Designer platforms, bundled Thai PDF fonts, example application coverage, and foundation rendering/resolver tests.

## PHASE 2 — DESIGNER V2

**Status: ✅ MERGED / COMPLETE — 2026-08-12**

Delivered save/load/import/export, Template Gallery, table column editing, snap/rulers/zoom, undo/redo, keyboard editing, and physical-mm persistence.

## PHASE 3 — PRINTER ENGINE

**Status: ✅ MERGED / COMPLETE — 2026-08-12**

Delivered Thai ESC/POS encoding/raster fallback, unified discovery, Sunmi adapter isolation, explicit cutter/cash-drawer capability contracts, and transport/render separation.

Physical printer compatibility remains a separate verification track.

## PHASE 4 — HARDENING

**Status: ✅ MERGED / COMPLETE — 2026-08-12**

Delivered CI hardening, rendering regression coverage, compatibility documentation, and full platform build validation.

## PHASE 5 — RELEASE

**Status: ✅ MERGED / COMPLETE — 2026-08-12**

Merge commit:

```text
be3378a69794732e12935ea594aa09a4a9b9e2e4
```

Release baseline includes pub.dev dry-run readiness and GitHub Pages deployment.

---

# Designer Stitch UI migration

Normalized implementation plan:

```text
docs/design/IMPLEMENTATION_PLAN.md
```

## P1 + P2 — Tokens / theme / shared controls

**Status: ✅ MERGED / COMPLETE**

PR #12.

## P3 + P4 — App shell / canvas primitives / live Designer migration

**Status: ✅ MERGED / COMPLETE**

PR #13 and PR #14.

## P5 — Inspector migration

**Status: ✅ MERGED / COMPLETE**

PR #15.

## P6 — Left panel / Layers / Data shell

**Status: ✅ MERGED / COMPLETE**

PR #16.

## P7 — Template Gallery migration

**Status: ✅ MERGED / COMPLETE**

PR #17.

Merge SHA:

```text
5077d517ef230a9cfa89fb8c376e6c2516481eb7
```

## P8 — Table UX expansion

**Status: ✅ MERGED / COMPLETE**

PR #18.

Merge SHA:

```text
18331aa9d667771ee4878b5e0c34db327bc9f774
```

## P9 — Unified Preview workspace

**Status: ✅ MERGED / COMPLETE — 2026-08-13**

P9 was intentionally split into PDF/system-print foundation and ESC/POS integration.

### P9A — PDF / System Print

PR #19 — `Designer P9A: unified PDF/system print preview workspace`

Merge SHA:

```text
99e0785c3a4032400c8c519102b5e2c0de5baa32
```

Delivered:

- unified `PreviewWorkspacePage`
- `DesignerAppShell`-based preview layout
- existing `FlutterReportPrinter.generatePdf()` as PDF source of truth
- platform System Print via `Printing.layoutPdf`
- actual paper metadata and generated byte status
- compact layout fallback
- print-error `InlineAlert`

Validation:

```text
CI run #138 — ✅ full 9-job matrix
```

### P9B — ESC/POS Preview Mode

PR #20 — `Designer P9B: ESC/POS preview mode`

Validated head:

```text
4cd8973b1cc66b32a3173519c877a3c50333267b
```

Merge SHA:

```text
9cfd4a3b31f69163c37aea13be4087cfa9e74d15
```

Delivered:

- `EscPosPreviewPanel`
- PDF ↔ ESC/POS output-mode switching
- bounded ESC/POS hex-byte preview and real byte count
- ESC/POS bytes rendered through the actual `EscPosRenderer.renderTemplate()` path
- `ReportTemplate.fromJson(...)` used for current Designer document data
- System Print hidden in ESC/POS mode
- ESC/POS mode offered only when a real payload exists
- Designer-side ESC/POS generation limited to thermal documents; A4/PDF are not silently converted
- transport remains unselected / no fake send action
- no invented cutter or cash-drawer UI
- hardware operations remain gated by `CutterCapability` / `CashDrawerCapability`

Validation:

```text
CI run #147 / run id 31685965055 — ✅ full 9-job matrix
```

Review state before merge:

- PR mergeable: ✅
- review threads: none
- final CI on current head: ✅

---

# Current architecture guardrails

## PDF

`FlutterReportPrinter.generatePdf()` / report engine remain the source of truth. System Print is delegated to the platform print dialog.

## ESC/POS

`EscPosRenderer` is transport-agnostic and returns bytes. Preview may display those bytes but must not imply that a printer is connected or selected.

## Transport

Actual sending requires an explicit `EscPosTransport`.

## Hardware capabilities

Only these generic optional hardware contracts currently exist:

```dart
abstract interface class CutterCapability {
  Future<void> cutPaper();
}

abstract interface class CashDrawerCapability {
  Future<void> openCashDrawer();
  Future<bool> isCashDrawerOpen();
}
```

Do not infer generic connected/status/encoding/hardware-support flags that are not represented by contracts.

---

# Validation baseline

Required CI gate remains:

- Designer format + analyze + tests
- `report_engine` format + analyze + tests + publish dry-run
- `report_engine_sunmi` format + analyze + tests
- report_engine example format + analyze + tests + Android APK
- Designer Web release
- Designer Android APK
- Designer Linux release
- Designer Windows release
- Designer macOS release
- Designer iOS simulator

Reference: Flutter **3.32.7**.

---

# Remaining release / hardware track

Software validation is green. Hardware-dependent compatibility still requires real-device evidence where available, including candidate thermal/Sunmi printers.

`report_engine_sunmi` remains a separate release decision from core `report_engine`.

---

# Current next action

The Stitch Designer plan currently ends at **P9**. **No P10 is defined.**

Before starting further implementation:

1. Re-read this handoff and `docs/design/IMPLEMENTATION_PLAN.md`.
2. Decide the next roadmap scope explicitly instead of inventing a P10.
3. Keep physical-printer verification as a separate evidence track.
4. Preserve the production/release baseline and full CI matrix for any next phase.
